import { GetFile } from "./GetFile";
import { connect } from 'react-redux';

const mapDispathToProps = (dispath) => {
    return {
    }
};

const mapStateToProps = (store) => {
    return {
    };
};

export default connect(mapStateToProps, mapDispathToProps)(GetFile);