import styled from 'styled-components';

const Container = styled.div`

`

export const Moderation = () => {
    return (
        <Container id="map">
            moderation
        </Container>
    );
}
